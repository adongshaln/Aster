# 设置精简（2.2.0-preview.1）

## 2.2.1-preview.1 补丁

- 提问上下跳转移至顶部标题栏，使用横排无背景图标和 48 dp 点击区域，不再悬浮遮挡正文。
- 多提问会话保持导航位置稳定；不可跳转的方向置灰。
- 回到底部取消投影，改为细描边与纯淡入淡出，避免尺寸过渡和阴影干扰阅读。
- versionCode 56；继续使用原固定签名，待真机验收后再合并 main。

- 设置首页以服务列表为主；默认模型和助手偏好按需展开，导入/导出保留在右上角配置管理中。
- 编辑顺序为连接信息、测试并同步、默认模型。仅保留顶部保存入口，返回仍提示放弃未保存修改。
- 工具默认值、连接恢复和高级设置默认折叠；展开标题与内部控件不再共用点击区域。
- 删除 GPT-5.6 专属优化面板、重复就绪检查和服务卡片的密钥尾号展示。
- GPT 对话及漫画分析请求统一走 Responses，包括旧配置、模型切换和带提供商前缀的模型 ID。
- 取消缓存策略暗中优先调用 Chat Completions；保留缓存兼容参数开关，命中仍由服务端决定。
- 非 GPT 模型仍可在高级设置选择协议；绘图协议、接口路径及自定义请求头保留。
- 继续使用固定签名；main 保持 2.1.0，待测试验收后再合并。

验证：新增 GPT 路由单元/请求测试及独立设置页 UI 测试。最终通过情况以 GitHub Actions 为准；真机需检查保存/返回、长表单键盘、工具开关与模型切换。

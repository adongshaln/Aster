# Changelog

Aster 的稳定版本更新记录。除特别说明外，只有经过测试并合并到 `main` 的改动才会记录在这里。

## 2.2.1 — 2026-09-05

### 改进
- 精简设置页：以服务列表为主，默认模型、助手偏好与高级选项按需展开。
- API 编辑页优先展示连接信息、测试同步与默认模型，导入/导出保留在配置管理入口。
- GPT 模型的对话与漫画分析请求统一使用 Responses API，旧配置和带提供商前缀的模型 ID 也遵循同一规则。
- 提问上下跳转移至聊天标题栏，减少对正文的遮挡。
- “回到底部”改为无投影的细描边胶囊，并采用轻量淡入淡出。
- 保留原包名、数据兼容性、固定签名，以及非 GPT 模型的兼容协议能力。

### 修复
- 移除含义不清晰的 GPT 专属优化面板，避免设置项重复和协议选择造成误解。
- 修复 GPT 启用缓存策略时可能先请求 Chat Completions 的问题。

### 验证与发布
- 用户真机验收通过。
- Release 单元测试与固定签名 APK 构建通过。
- 正式版本为 2.2.1，versionCode 56。

## 2.1.0 — 2026-09-05

### 改进
- 全面升级原生界面，统一暖白、浅棕与墨色配色、字体层级、留白、图标及卡片样式。
- 重构聊天欢迎页与灵感建议入口，整理输入胶囊、模型切换及输入选项面板。
- 更新侧栏、创作、媒体下载与设置页布局，统一主要操作入口与信息分组。
- 保留双 API 协议、工具调用、键盘同步、底部渐进模糊和已有数据兼容性。

### 修复
- 移除输入栏加号旁含义不明确的棕色工具数量角标；工具状态仍可在输入选项面板中查看和修改。

### 验证与发布
- 预览版签名 Release 构建与单元测试通过，并经用户真机测试确认后转为正式版。
- 新增原生 UI 预览测试及工作流；自动 UI 预览检查尚未通过，不计作已完成的验证。
- 正式版本为 2.1.0，versionCode 54；继续使用原包名与固定签名配置。

## 2.0.0 — 2026-09-05

### 新增
- 应用正式更名为 Aster，并更新应用名称、主题与相关保存路径。
- 聊天输入栏支持聚焦后展开，提供更大的输入空间。
- GitHub Actions 支持自动测试、构建、固定密钥签名 APK，并允许开发分支直接产出测试包。
- `v*` 标签可用于自动发布 GitHub Release。

### 改进
- 优化启动器 Adaptive Icon 的前景安全边距，避免 Aster Logo 在不同启动器蒙版下被裁切。
- 优化聊天页 Haze 毛玻璃渲染：静止时保留真实模糊，滚动、键盘动画和流式生成期间切换到轻量渐变以降低 GPU 压力。
- 将输入栏多个独立尺寸动画合并为单一动画进度，减少布局抖动和重复重组。
- 降低流式 Markdown 活跃解析区域，减少长回复生成期间的重复排版开销。
- 流式生成中的最后一条消息不再运行列表 Item 动画，改善聊天页整体流畅度。
- 输入法展开和收起过程中，消息列表会持续保持在底部并同步移动。

### 修复
- 修复桌面图标过度放大，导致 Logo 显示不完整的问题。
- 修复用户收起输入法后，输入栏仍保持展开状态的问题。
- 修复打开输入栏时聊天内容不会随键盘同步向上滚动的问题。

### 构建与发布
- 正式 APK 使用固定签名密钥构建，可用于后续覆盖安装。
- Android CI 使用 Java 17、Android 36，并启用 Gradle 缓存。
- `main` 继续作为确认过的稳定版本；试验性修改优先在开发分支构建 APK，确认后再合并。

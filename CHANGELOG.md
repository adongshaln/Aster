# Changelog

Aster 的稳定版本更新记录。除特别说明外，只有经过测试并合并到 `main` 的改动才会记录在这里。

## 未发布

## 2.5.1 — 2026-09-13

- 修复宏匹配表达式未转义右花括号、在 Android ICU 正则引擎初始化时抛出异常的问题；启用正则与预设宏不再因此闪退。
- 故事正文的显示正则改为后台计算，逐项配置修改后立即重新计算。
- HTML 预览复用当前 WebView 更新内容，销毁后取消延迟加载与绘制回调，避免访问已释放的预览。
- 正则捕获组展开时检查输出上限，超限规则保留原文并继续后续规则。
- 增加真实内置预设正文渲染、连续更新和超大替换回归用例。
- 保持原发布签名，版本号为 2.5.1 / 61。

## 2.5.0 — 2026-09-12

### 故事生成恢复
- 失败、超时或未完整结束的故事回复提供“重新生成”，复用原用户输入且不创建重复剧情消息。
- 每次重试创建新的流式修订；旧失败内容继续保留在版本历史中，只有完整完成的新版本才进入正式记忆。

### 酒馆预设
- 故事正文支持导入、选择、停用与删除 SillyTavern JSON 预设，并内置 `Izumi 1th Anniv1`。
- 新增完整预设配置页：展示文件内全部提示词和正则的默认启用 / 停用状态，支持搜索、筛选、逐项选择、内容查看和恢复文件默认值。
- 执行 `prompt_order`、角色顺序、常用变量 / 随机宏、兼容采样参数，以及 Regex 的请求清理与回复显示路径。
- Regex HTML 使用无脚本、无网络沙箱；Tavern Helper 脚本随原文件保留但不会执行。

### 验证与发布
- Android Build #287：单元测试与 Release 编译通过，内置预设资源按真实文件完成解析与默认状态校验。
- Native UI preview #64：15 项原生交互测试全部通过，包括预设默认值、筛选、逐项切换与正则详情。
- 签名证书 SHA-256 保持为 `3e4da1d062819d9f1065f85654de71ae5f0ad93aa10c52fc85faad0338a4e3b1`，支持覆盖安装。
- 保留 `com.adong.adchat` 应用 ID、现有配置与历史数据兼容性。
- 正式版本为 **2.5.0，versionCode 60**。

## 2.4.1 — 2026-09-12

### 委托联网搜索
- Chat Completions 模型可通过单独选择的 Responses 兼容搜索模型执行实时 Web Search，并可选启用 X Search。
- 仅把独立搜索问题发送给搜索后端，不发送完整聊天历史；原对话模型继续负责最终回答。
- 保留搜索后端返回的来源与 URL 引用，并将其纳入现有聊天引用展示。
- 同一轮内复用相同搜索，真实后端搜索最多 4 次，避免重复请求和费用失控。

### 工具兼容与防误报
- 委托搜索可与 Skill、`create_file` 及其他 Chat 函数工具在同一轮中共存。
- 修复配置委托搜索后切换 Chat 协议可能错误关闭文件工具的问题。
- 只有响应中真实出现对应的 `web_search_call` 或 `x_search_call` 才认定搜索成功，拒绝把普通文本响应伪装成搜索结果。
- 旧配置保持搜索关闭，直到用户明确选择搜索 Profile 和模型。

### 验证与发布
- Android Build #278：单元测试与 Release 编译通过，生成固定签名 APK。
- 签名证书 SHA-256 保持为 `3e4da1d062819d9f1065f85654de71ae5f0ad93aa10c52fc85faad0338a4e3b1`，支持覆盖安装。
- 保留 `com.adong.adchat` 应用 ID、现有配置与历史数据兼容性。
- 正式版本为 **2.4.1，versionCode 59**。

## 2.4.0 — 2026-09-09

### 故事模式
- 正式加入独立故事模式，提供讨论 / 正文工作区、故事档案与独立 SQLite 数据层，不改写普通聊天历史。
- 自动整理人物、关系、人物认知、当前状态、剧情事实、作者计划、候选与长篇摘要，并保留来源与版本信息。
- 支持讨论结论应用、候选编辑后确认、资料疑似冲突处理、关联决定复核与独立设定重新适用。
- 支持历史正文模型重写、修改旧请求后从历史节点另写、路线切换，以及受限的撤销 / 恢复。
- 完整正文才有资格进入正式记忆；停止、失败和不完整输出不会被当作已确认事实。
- 整理任务、失败恢复、用量与版本状态持久化，并保护固定资料、未整理正文及必要上下文不被静默裁掉。

### 共享聊天 UI 与交互
- 普通聊天和故事模式统一使用 ConversationComposer、ConversationHeader、等待动画、附件、长草稿编辑器、消息操作和阅读基础组件。
- 修复故事模式早期用户气泡内容堆叠、模型回复默认折叠、等待状态缺失、详情操作缺少按钮样式等问题。
- 详情内容默认收在回复底部入口内；展开详情会主动保持新内容可见。
- 输入框聚焦、提示词、光标布局、键盘关闭收缩和 IME 跟随行为与普通聊天统一。
- 欢迎页、侧边栏、消息阅读层级、长草稿编辑器与操作入口进行一轮全局重设计。
- 保留稳定底部阅读渐变，不重新启用曾在部分设备产生静止 / 触摸差异的实时毛玻璃路径。

### 按模型上下文预算
- 每个 API Profile 下按精确模型 ID 独立保存上下文窗口与输出上限。
- 普通聊天与故事模式共享同一预算、裁剪和恢复策略。
- 模型选择器只为当前选中的模型显示 128K / 256K / 512K / 1M 快捷档位，其他模型保持紧凑。
- 设置页保留精确数值编辑、恢复默认与配置导入 / 导出。
- 请求预算计入输出预留、工具与图片估算；不会根据模型名称猜测提供商真实容量。

### HTML 内容预览
- 完整 HTML / HTM 代码块生成结束后默认直接显示内容预览，不再在卡片主体外显源码。
- 保留复制 HTML 源码、保存 `.html` 与全屏预览。
- 流式 / 未闭合 HTML 只显示生成中状态，完成后再创建预览。
- HTML 预览采用受限制的离线 WebView：禁用外部网络、文件 / content 访问、DOM 存储、新窗口与父页面导航。
- 普通聊天与故事模式共用同一 HTML 渲染组件。

### PDF 与 Office 文件
- `create_file` 新增真实 PDF、DOCX、XLSX、PPTX 二进制生成，并继续支持 HTML、Markdown、文本、JSON 与 CSV。
- PDF 支持 A4、中文系统字体、自动换行、分页、页码与长表格按行分页。
- DOCX 支持可编辑正文、标题、列表与表格，并保留 Unicode。
- XLSX 支持多工作表、数字 / 布尔类型、冻结首行、表头样式与基础公式。
- PPTX 支持可编辑 16:9 标题 / 要点页面与完整 Office 包关系。
- 二进制附件随 ConversationStore 持久化，导出时恢复原始 bytes，不再把二进制编码写成文本文件。
- 已知边界：PDF 页面视觉显示正确，但 Android / Skia 的部分中文文字层复制或提取不保证逐字符等于原始 Unicode；DOCX 不受此限制。

### 技能包运行时
- 支持从公开 GitHub 地址安装或从 ZIP 导入完整 Skill，并在普通聊天、故事讨论与正文工作区分别选择。
- Chat Completions 与 Responses 均可按需执行 `load_skill` 和 `read_skill_file`，参考文件在模型实际需要时才进入上下文。
- GitHub Skill 使用固定提交文件清单和按需下载，不再要求缓存整个仓库；本地 ZIP 继续使用严格的路径、文件数与大小限制。
- 修复同批次先读取后加载、重复读取、删除后仍占选择名额以及停用后无法取消选择的问题。
- Skill 文件改为一次返回完整 UTF-8 文本；每轮复用重复读取，并保留 16 轮、48 次工具调用的独立安全限制。
- 当前运行时只读取 Skill 资料并调用 Aster 已有工具，不执行 Python、Node、Shell、浏览器自动化或 Skill 内脚本。

### 验证与发布
- 用户在真实 Android 设备上完成开发版日常使用验收并确认可发布。
- Android Build #169：单元测试、Release 编译与固定签名成功。
- Native UI preview #57：14 项原生 UI 测试，0 failures / errors / skipped。
- 独立验证确认 Android 生成的 PDF、DOCX、XLSX、PPTX 包结构与持久化有效。
- 继续保留 `com.adong.adchat` 应用 ID、既有配置兼容性与原固定签名更新链路。
- 正式版本为 **2.4.0，versionCode 58**。

## 2.3.0 — 2026-09-05

### 阅读体验
- 重做聊天正文排版，面向小说、角色扮演与日常长回复优化字号、行高、段落间距与阅读密度。
- 流式回复采用更连续的出字节奏与平滑跟随滚动，减少原始 Token 批次造成的跳动感。
- 用户手动滚动后停止自动抢滚动，继续保留“跟随生成 / 回到底部”入口。
- 保留模型明确换行与对白结构，提升小说场景下的段落可读性。
- Markdown 标题扩展到 H1–H6，修复 `####` 等较低层级标题直接显示井号的问题。
- `『术语』` 连同符号使用 Aster 暖棕色强调，方便标记世界观名词、专有设定与重要概念。
- Assistant 回复尾部的模型、Token、缓存等技术信息默认收进“详情”，降低对正文阅读的干扰。

### 动效与界面
- 模型等待状态改用透明 Aster 星标动画：原地轻跳，并在空中逐次旋转 90°。
- Thinking 状态加入“Aster 正在思考 / 正在组织回答…”两级信息，并与正文开始自然衔接。
- 流式正文尾部使用 Aster 标记替代传统文本光标。
- 底部阅读区域固定采用稳定渲染路径，修复静止与按住/滚动状态之间毛玻璃效果不一致、发白或透底异常的问题。
- 模型切换、新建对话等瞬时提示移到顶部区域，不再遮挡输入栏；整条提示可点击立即关闭，新提示会替换旧提示。

### 兼容与保留
- 保留 `com.adong.adchat` 应用 ID、既有配置格式、历史对话与固定签名更新链路。
- 保留已验收的 IME 同步上移、顶部提问导航、悬浮输入胶囊及 GPT Responses API 路由策略。

### 验证与发布
- 阅读体验、术语高亮、底部渲染与顶部提示均经用户真机验收通过。
- Release 单元测试与固定签名 APK 构建通过。
- 正式版本为 2.3.0，versionCode 57。

## 2.2.1 — 2026-09-05

### 改进
- 精简设置页：以服务列表为主，默认模型、助手偏好与高级选项按需展开。
- API 编辑页优先展示连接信息、测试同步与默认模型，导入/导出保留在配置管理入口。
- GPT 模型的对话与漫画分析请求统一使用 Responses API，旧配置和带提供商前缀的模型 ID 也遵循同一规则。
- 提问上下跳转移至聊天标题栏，减少对正文的遮挡。
- “回到底部”改为无投影的细描边胶囊，并采用轻量淡入淡出。
- 保留原包名、数据兼容性、固定签名，以及非 GPT 模型的兼容协议能力。

### 修复
- 移除含义不清晰的 GPT 专属优化面板，避免设置项重复和协议选择造成误解。
- 修复 GPT 启用缓存策略时可能先请求 Chat Completions 的问题。

### 验证与发布
- 用户真机验收通过。
- Release 单元测试与固定签名 APK 构建通过。
- 正式版本为 2.2.1，versionCode 56。

## 2.1.0 — 2026-09-05

### 改进
- 全面升级原生界面，统一暖白、浅棕与墨色配色、字体层级、留白、图标及卡片样式。
- 重构聊天欢迎页与灵感建议入口，整理输入胶囊、模型切换及输入选项面板。
- 更新侧栏、创作、媒体下载与设置页布局，统一主要操作入口与信息分组。
- 保留双 API 协议、工具调用、键盘同步、底部渐进模糊和已有数据兼容性。

### 修复
- 移除输入栏加号旁含义不明确的棕色工具数量角标；工具状态仍可在输入选项面板中查看和修改。

### 验证与发布
- 预览版签名 Release 构建与单元测试通过，并经用户真机测试确认后转为正式版。
- 新增原生 UI 预览测试及工作流；自动 UI 预览检查尚未通过，不计作已完成的验证。
- 正式版本为 2.1.0，versionCode 54；继续使用原包名与固定签名配置。

## 2.0.0 — 2026-09-05

### 新增
- 应用正式更名为 Aster，并更新应用名称、主题与相关保存路径。
- 聊天输入栏支持聚焦后展开，提供更大的输入空间。
- GitHub Actions 支持自动测试、构建、固定密钥签名 APK，并允许开发分支直接产出测试包。
- `v*` 标签可用于自动发布 GitHub Release。

### 改进
- 优化启动器 Adaptive Icon 的前景安全边距，避免 Aster Logo 在不同启动器蒙版下被裁切。
- 优化聊天页 Haze 毛玻璃渲染：静止时保留真实模糊，滚动、键盘动画和流式生成期间切换到轻量渐变以降低 GPU 压力。
- 将输入栏多个独立尺寸动画合并为单一动画进度，减少布局抖动和重复重组。
- 降低流式 Markdown 活跃解析区域，减少长回复生成期间的重复排版开销。
- 流式生成中的最后一条消息不再运行列表 Item 动画，改善聊天页整体流畅度。
- 输入法展开和收起过程中，消息列表会持续保持在底部并同步移动。

### 修复
- 修复桌面图标过度放大，导致 Logo 显示不完整的问题。
- 修复用户收起输入法后，输入栏仍保持展开状态的问题。
- 修复打开输入栏时聊天内容不会随键盘同步向上滚动的问题。

### 构建与发布
- 正式 APK 使用固定签名密钥构建，可用于后续覆盖安装。
- Android CI 使用 Java 17、Android 36，并启用 Gradle 缓存。
- `main` 继续作为确认过的稳定版本；试验性修改优先在开发分支构建 APK，确认后再合并。
